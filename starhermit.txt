# Starhermit game manifest — github.com/HypeDriven/starhermit-football
# Read by the Starhermit backend when this repository is added as a game
# (Add game -> paste the repo URL in the Starhermit client).
# Format: key=value lines; '#' comments and blank lines are ignored.

name=StarHermit Football

# URL-safe identifier for this game on the platform. The game client reads its
# own slug from the launch token, so nothing is hard-coded to it.
slug=football

# Repo-relative HTML entry point the client loads.
launch=index.html

# The Starhermit account that owns this game (the submitter's user id).
owner=2712e04e-461b-4d23-81ae-e40b429128a8

# The authoritative match simulation. The platform runs this script server-side
# in its Jint sandbox, ticking it at the game's configured tick rate (30 Hz) via
# the platform's per-game tick-rate support — see spec.md section 3. Realtime
# Rooms (/api/v1/realtime, /ws/v1/realtime) are used for lobby/roster only.
server=server.js

# Default desktop-browser controls (spec.md sections 5.6 and 8.8). Players can
# rebind these per game from the platform client's game-details page; on launch
# in a non-mobile browser the game fetches the player's effective bindings via
# GET /api/v1/games/<slug>/controls and remaps its keyboard handling to them.
# Tank-style steering: up/down run forward/back, left/right rotate the player.
# The shoot key is contextual: a quick tap passes, holding charges a shot.
# Format: control.<action>=<KeyboardEvent.code>[+<code>...] | <label>
control.up=KeyW+ArrowUp | Run forward
control.down=KeyS+ArrowDown | Backpedal
control.left=KeyA+ArrowLeft | Turn left
control.right=KeyD+ArrowRight | Turn right
control.shoot=Space+KeyJ | Shoot (hold) / tap to pass
control.pass=KeyL | Pass
control.tackle=KeyK | Slide tackle / pressure
